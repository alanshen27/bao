# Bao uses local-first SQLite storage under .bao/ at the repo root.
# This path is relative to the Prisma schema directory (prisma/), so it
# resolves to <repo-root>/.bao/bao.db. The gateway sets an absolute
# DATABASE_URL at runtime that points to the same physical file.
DATABASE_URL="file:../.bao/bao.db"
